/*
El costo de un desarrollo de un proyecto de software se calcula en base al lenguaje que se necesita:

Nombre  Tipo    Valor hora
C/C++   'C'         $ 2500
C#      '#'         $ 2100
Python  'P'         $ 1400
Go      'G'         $ 2000

Además, si el proyecto es marcado como Urgente, se le aumenta un 120 % más al costo del proyecto.
Le solicitan un programa que permita calcular el costo total de un proyecto basado en la cantidad de horas (int),
 el tipo de lenguaje (char) y si es urgente o no (bool).
*/

#include<iostream>

using namespace std;



int main ()
{
int canthoras,valorhora,total;
char proyecto;
bool urgente;


    cout<< "ingresar cantidad de horas requeridas para el proyecto: ";
    cin >> canthoras;
    cout<<endl<< "Elegir el tipo de Lenguaje del proyecto: "<<endl;
    cout<< " 'C' para C/C++ , '#' para C# , 'P' para Python y 'G' para Go "<<endl;
    cin >> proyecto;

    switch(proyecto)
            { case 'C':
              case 'c':
                        total = canthoras*2500;
                break;
              case '#':
                        total = canthoras*2100;
                break;
              case 'P':
              case 'p':
                        total = canthoras*1400;
                 break;
              case 'G':
              case 'g':
                        total = canthoras*2000;
                 break;
                    default: cout << "El Caracter ingresado es incorrecto." << endl;
                        break;
            }


                cout << "El proyecto es urgente? " << endl<<"1 - Sí    0 - No" << endl;
                cin >> urgente;

                if (urgente==1)
                {
                    total*=2.2;
                }

                cout << "El Monto total a pagar seria: " << total;


return 0;}
